package com.geoteam.geostory;

import android.location.Location;
import android.location.LocationListener;
import android.os.Bundle;



//Dummy files. Needed to fulfil interface but never actually used.
//These need to exist, but any logic that would call these is handled
//Elsewhere, due to the fact that location is only accessed when necessary.
//To fully implement this interface would be redundant, and less battery-efficient.
public class LocationListener_custom implements LocationListener {

	@Override
	public void onLocationChanged(Location loc)
	{
		//TODO
	}
	
	@Override
	public void onProviderDisabled(String Provider)
	{
		//TODO
	}
	
	@Override
	public void onProviderEnabled(String Provider)
	{
		//Todo
	}
	
	@Override
	public void onStatusChanged(String Provider, int status, Bundle extras)
	{
		//Do Nothing
	}

}
